package pomo.Lockedin.repository;

import org.springframework.data.repository.CrudRepository;
import pomo.Lockedin.entities.RevisionTopic;

public interface RevisionTopicRepository extends CrudRepository<RevisionTopic, Long> {
}
